// Custom hooks barrel export.
// Add hooks here as the application grows.
//
// Future hooks:
//   useAuth — convenience wrapper around authStore
//   useSignalR — manage SignalR connection lifecycle
//   usePayment — Razorpay checkout flow
//   useExpenses — expense CRUD with optimistic updates
//   useDebounce — debounced value for search inputs

export { useAuthStore } from '../store/authStore';
